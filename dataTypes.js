// ------Data Types------
const userName = "Steven"; // string
const userAge = 25; // number
const userAdult = true;
const userBalance = null;
const userFriends = ["Mark", "Ricky", "Shaun"];
let userLicense;

// console.log(userName + userAge);
// math operator = +, -, /, *, %, **, ++, --
// console.log(userAge + 2);
// console.log(userFriends.length);

// there 8 data types in javascript
// 7 primitive = number, string, boolean, null, undefined, symbol, Bigint.
// 1 object/non-primitive/reference = array, object literals, sets, map, function

// console.log();
// console.log(userName.slice(0, 1) + userName.slice(-1));
// console.log((20 + 10) * 4);

// operands

// expression
// 10 + 20

// statement
// const sum = 10 + 30 + 60 / 90;
// document.querySelector("body").style.backgroundColor = "red";
