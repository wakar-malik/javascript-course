// math operators ->> +, -, /, *, **, ++, --, %
// comparison operator ->> >, <, >=, <=,= =, ===, ?
// assignments operators ->> =, +=, -=, *=, **=, /=
// logical operators ->> ||, &&, ??

// (logical) !
// (math) ---- inc/dec > exponent > multi/divide/module > add/sub
// (compare) ---- </<=/>/>= > equality/not equality
// (logical) && > ||/nullish
// (compare) ---- ternary

console.log(2 + 2 * 5);
console.log((2 + 2) * 5);
